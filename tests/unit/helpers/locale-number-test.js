import { localeNumber } from '../../../helpers/locale-number';
import { module, test } from 'qunit';

module('Unit | Helper | locale number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = localeNumber([42]);
  assert.ok(result);
});
